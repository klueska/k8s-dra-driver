apiVersion: resource.nvidia.com/v1beta1
kind: ComputeDomain
metadata:
  name: imex-channel-injection-${INDEX}
spec:
  numNodes: 1
  channel:
    resourceClaimTemplate:
      name: imex-channel-injection-${INDEX}
